apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-workspace
  namespace: ai-dev
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  # Uncomment and set storageClassName for your cluster:
  # storageClassName: nfs
